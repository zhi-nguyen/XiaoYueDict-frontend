# Standard Next.js Environment Variables
# The base URL of the backend API Gateway (Nginx)
NEXT_PUBLIC_API_URL=http://localhost

# The WebSocket URL (if applicable)
NEXT_PUBLIC_WS_URL=ws://localhost/ws
